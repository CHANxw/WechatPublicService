.container
    .row
        .page-header.clearfix
            a(href='/', style='text-decoration: none')
                h1= title
            .col-md-4
                if user
                    small= user.name + '大大'
                else
                    a(href='#', data-toggle='modal', data-target='#signinModal') 登录进入管理
            .col-md-8
                form(method= 'GET', action='/results')
                    .input-group.col-sm-4.pull-right
                        input.form-control(type='text', name='search', placeholder='电影名称或分类进行查询')
                        span.input-group-btn
                            button.btn.btn-default(type='submit') 搜索
    .navbar.navbar-default.navbar-fixed-bottom
        .container
            .navbar-header
                a.navbar-brand(href='/') 电影mongo
            if (user)
                p.navbar-text.navbar-right
                    span 欢迎#{user.name}进入电影mongo
                    span &nbsp;|&nbsp;
                    a.navbar-link(href='/logout') 退出账号
            else
                p.navbar-text.navbar-right
                    a.navbar-link(href='#', data-toggle='modal', data-target='#signupModal') 注册
                    span &nbsp;|&nbsp;
                    a.navbar-link(href='#', data-toggle='modal', data-target='#signinModal') 登录
                    // modal 是bootstrap的插件 -- 模态框  使用方法参考http://www.runoob.com/bootstrap/bootstrap-modal-plugin.html
#signupModal.modal.fade
    .modal-dialog
        .modal-content
            form(method='POST', action='/user/signup')
                .modal-header 注册
                .modal-body
                    .form-group
                        label(for='signupName') 用户名
                        input#signupName.form-control(type='text', name='user[name]')
                    .form-group
                        label(for='signupPassWord') 密码
                        input#signupPassWord.form-control(type='password', name='user[password]')
                .modal-footer
                    button.btn.btn-default(type='button', data-dismiss='modal') 关闭
                    button.btn.btn-success(type='submit') 提交
#signinModal.modal.fade
    .modal-dialog
        .modal-content
            form(method='POST', action='/user/signin')
                .modal-header 登录
                .modal-body
                    .form-group
                        label(for='signinName') 用户名
                        input#signinName.form-control(type='text', name='user[name]')
                    .form-group
                        label(for='signinPassWord') 密码
                        input#signinPassWord.form-control(type='password', name='user[password]')
                .modal-footer
                    button.btn.btn-default(type='button', data-dismiss='modal') 关闭
                    button.btn.btn-success(type='submit') 提交
